server:
  port: 9100
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
    password: rajureddy98
    platform: postgres
   #url: jdbc:postgresql://host.docker.internal:5432/faculty #docker
    url: jdbc:postgresql://@facultyy.ctgdmuvntgrx.ap-south-1.rds.amazonaws.com:5432/facultyy?user=postgres&password=rajureddy98
    username: postgres
  jackson:
    serialization:
      fail-on-empty-beans: false
  cloud:
    config:
      override-none: true
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
    show-sql: true
eureka:
    client:
      serviceUrl:
         #defaultZone: http://netflix-eureka:8761/eureka #docker
          defaultZone: http://65.0.103.248:30001

